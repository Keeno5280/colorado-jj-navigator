import React from 'react';
import ChargeTypes from '../components/ChargeTypes';

const ChargeTypesPage: React.FC = () => (
    <div className="page-wrapper">
        <ChargeTypes />
    </div>
);

export default ChargeTypesPage;
